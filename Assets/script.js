var startButton = document.querySelector(".startButton");

// Make box for question
var question = document.createElement('div')
 
// Make questions:
function question1 (choice1, choice2, choice3, choice4) {
// Create div element to hold each question
    var question = document.createElement("div");
    var node = document.createTextNode("What are the four main types of data?");
    question.appendChild(node);

    // var emptyString = "";
 
}; 

function question2 (choice1, choice2, choice3, choice4) {
    var question = document.createElement('div');
};

function question3 (choice1, choice2, choice3, choice4) {
    var question = document.createElement('div');
};

function question4 (choice1, choice2, choice3, choice4) {
    var question = document.createElement('div');
    let question = 
};


startButton.addEventListener("click", function () {
// Upon clicking the start button, needs to show 5 divs (question, choice1, choice2, choice3, choice4)
    return question1();

});